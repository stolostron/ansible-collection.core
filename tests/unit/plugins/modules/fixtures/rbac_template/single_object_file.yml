apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: unused
  namespace: unused
rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    verbs:
      - get
      - create
      - update
      - patch
      - delete
